## layout the order in which topics are presented in the rd docs
# 1. get a list of topics with the following:
#    db <- tools::Rd_db("SpatDiv")
#    topics <- gsub("\\.Rd", "", names(db))
#    cat(paste(topics, collapse = "\n"))
# 2. arrange the topic names into sections as desired in the format below:
## If you need to see which topics are missing, do the following in addition:
# cur <- yaml::yaml.load_file("rd_index.yaml")
# cur <- unlist(lapply(cur, function(x) x$topics))
# cat(paste(setdiff(topics, cur), collapse = "\n"))
knitr:
  eval: true

sections:
- 
  section_name: Package description
  topics:
    -
      file: SpatDiv.Rd
      title: "SpatDiv: SpatDiv, SpatDiv-package"
- 
  section_name: Diversity Accumulation
  topics:
    - DivAccum
    - EntAccum
    - Mixing
    - plot.Accumulation
- 
  section_name: Maps
  topics:
    -
      file: MapPlot.Rd
      title: "MapPlot: MapPlot, MapPlot.Accumulation"
- 
  section_name: Communities
  topics:
    - rSpCommunity
    -
      file: SpeciesDistributions.Rd
      title: "SpeciesDistributions: SpeciesDistributions, as.SpeciesDistribution.wmppp, as.SpeciesDistribution.factor, as.SpeciesDistribution.character, as.ProbaVector.wmppp, as.ProbaVector.factor, as.ProbaVector.character, as.AbdVector.wmppp, as.AbdVector.factor, as.AbdVector.character"
- 
  section_name: Entropy and Diversity
  topics:
    - Tsallis.character
    - Tsallis.factor
    - Tsallis.wmppp
    - Diversity.character
    - Diversity.factor
    - Diversity.wmppp
- 
  section_name: Utilities
  topics:
    - parallelCountNbd
